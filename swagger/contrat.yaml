swagger: '2.0'
info:
  version: "1.0.1"
  title: HandsOn EndPoints
  description: This is an example of an Openapi definition with Cloud Endpoints implementation.
security: 
  - APIKeyQueryParam: []
produces:
  - application/json
x-google-backend:
  address: https://helloworld-un6nz7ltbq-uc.a.run.app
host: "helloworld-un6nz7ltbq-uc.a.run.app"

paths:
  /version:
    get:
      summary: return current version
      description: float current version
      operationId: version
      produces:
      - application/json
      responses:
        200:
          description: OK
          schema:
            type: array
            items:
              type: string

  /ping:
    get:
      summary: Server heartbeat operation
      description: application heartbeat
      operationId: ping
      produces:
      - application/json
      responses:
        200:
          description: OK
          schema:
            type: string
securityDefinitions:
  APIKeyQueryParam:
    type: apiKey
    name: api_key
    in: query
# Added by API Auto Mocking Plugin
basePath: /api/v1
# Added by API Auto Mocking Plugin
schemes:
 - https
